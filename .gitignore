# Secrets
.env
config_local.py

# Training artifacts (large, regenerable)
runs/

# Dataset (too large for GitHub — keep local / in Drive)
shezhen_data/
shezhen_datasets1.zip

# Trained models ARE tracked (tongue_yolo_best.pt is ~6MB, well under the 100MB limit).
# Exclude only oversized checkpoints:
models/*_last.pt
models/epoch*.pt

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Streamlit
.streamlit/secrets.toml

# OS
.DS_Store
Thumbs.db

# DB (regenerated on first run)
*.db
